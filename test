1、char *s=”aaa”,aaa字符串是位于内存中的字符串常量区域，当在系统编译时，就已经存在。当运行时，会分配一个指针单元，让其指向这个字符串常量。而且不能通过指针s修改其值，否则系统就会崩溃。而char s[]是创建一个指向数组的指针，可以通过下标对数组的值进行运算改变。
2、所谓面向对象的程序设计，就是把面向对象的思想应用到软件工程中，并指导开发维护软件。对象是由数据和容许的操作组成的封装体，所谓面向对象，就是基于对象的概念，以对象为中心，类和继承为构造机制，认识了解刻画客观世界以及开发出相应的软件系统。
